spring.application.name=vrunk-backend

# --- Datasource (valeurs par défaut = tes infos) ---
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:vrunkdb}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:bxbjimg12526}
spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- CORS ---
allowed.origins=http://localhost:4200,https://vrunkV2.com

# --- Spring Data REST ---
spring.data.rest.base-path=/api

# --- Divers ---
stripe.enabled=false
stripe.key.secret=DUMMY_KEY
server.ssl.enabled=false
server.port=${SERVER_PORT:8080}

